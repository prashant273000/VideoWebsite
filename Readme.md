A Video Website

Nodemon is the  dev dependency and not pushed into the production it starts the server after the file is pushed

npm run dev executes the command from the package.json

ending slash not needed in the mongodb .env url

Database is always in other continent so use promise or try catch and async/await

//* CORS ORIGIN is used to allow everyone